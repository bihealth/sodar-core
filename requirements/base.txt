# Setuptools
setuptools>=84.0.0, <85.0

# Packaging
packaging>=26.3, <27.0

# Django
django>=5.2.17, <6.0

# Configuration
django-environ>=0.14.0, <0.15

# Forms
django-crispy-forms>=2.7, <2.8
crispy-bootstrap4==2026.2

# Models
django-model-utils>=5.0.0, <5.1

# Password storage
argon2-cffi>=25.1.0, <25.2

# Python-PostgreSQL Database Adapter
psycopg2-binary>=2.9.13, <2.10

# Unicode slugification
# NOTE: Replaces awesome-slugify, see #1547
python-slugify>=9.0.0, <9.1

# SVG icon support
django-iconify>=0.5.0, <0.6

# OpenID Connect (OIDC) authentication support
social-auth-app-django>=6.0.1, <6.1

# Login security
django-axes[ipware]>=8.3.1, <8.4

# Online documentation via django-docs
# NOTE: Sphinx requires docutils<0.23
docutils==0.22.4
Sphinx==9.1.0
sphinx-rtd-theme==3.1.0

# Versioning
setuptools-scm>=10.2.3, <10.3

######################
# Project app imports
######################

# Django-plugins (with Django v4.2 support)
django-plugins-bihealth>=0.5.2, <0.6

# Rules for permissions
rules>=3.5, <3.6

# REST framework
djangorestframework>=3.18.1, <3.19

# Keyed list addon for DRF
drf-keyed-list-bihealth>=0.2.1, <0.3

# Token authentication
django-rest-knox>=5.1.0, <5.2

# Markdown field support
markdown==3.10.3
# Replaced by Martor, still needed for migrations. To be removed in v2.1.
django-markupfield>=2.0.1, <2.1
mistune>=3.3.4, <3.4
martor>=1.8.2, <1.9

# Database file storage for filesfolders
django-db-file-storage==0.5.6.1

# Celery dependency
redis>=8.1.0

# Backround Jobs requirements
celery>=5.6.3, <5.7

# Django autocomplete light (DAL)
django-autocomplete-light>=5.0.0, <5.1

# DRF-spectacular for OpenAPI schema generation
drf-spectacular>=0.30.0, <0.31
