/* Alternative JS theme for martor
 *
 * By default, martor loads Bootstrap v5. We already use Bootstrap v4 in
 * SODAR Core, so we provide this as an alternative file for martor where we
 * can supply additional JS as needed.
 */
